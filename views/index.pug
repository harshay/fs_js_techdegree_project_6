extends layout.pug

block content 
  article.grid-container.portfolio-intro
    div

      //- **done** Add a title or heading like My Portfolio, or Hey, Potential Employers, or Let's make the web great together! 
      h1= index_heading

      //- **done** Add a description for your portfolio, what this project is, it's purpose, what it's supposed to accomplish, etc. 
      p.lead.text-light= portfolio_description

  article.grid-container.portfolio-index
    
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3

      for x in data_obj_array
        .cell
          a(href='#')
            img.thumbnail(src=`${x.img_urls[0]}`)
            h5 x.project_name

//-
      
      
